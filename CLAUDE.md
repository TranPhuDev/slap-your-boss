# Claude Instructions

Read and obey `AGENTS.md`.

Only implement the single feature marked `State: active` in `harness/FEATURE_LIST.md`.

Run `node scripts/harness-check.mjs` before editing and update harness files after verification.
